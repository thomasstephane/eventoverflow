<%= form_for [@event, @comment] do |f| %>
  <p>
    <%= f.label :comment %><br />
    <%= f.text_field :comment %>
  </p>
  <%= f.submit %>

<% end %>

<% if @errors %>
  <ul>
    <% @errors.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>